<% title_tag [t('pages.comments'), @page.title].join(' · ') %>

<div class="row">
  <%= render 'sidebar' %>

  <div class="col-md-9 space-y-6">
    <div class="card">
      <div id="comments" class="card-body text-lg">
        <%= link_to @page.title, page_path(@page.slug) %> - <%= t('pages.comments') %>
      </div>
    </div>

    <div class="hide-ios space-y-6">
      <%= render "/shared/comments", commentable: @page %>
    </div>
  </div>
</div>
